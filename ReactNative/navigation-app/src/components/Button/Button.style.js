import { StyleSheet } from "react-native-web";

export default StyleSheet.create({
  container: {
    backgroundColor: "#bbb",
    borderRadius: 5,
    padding: 5,
    margin: 10,
    alignItems: "center",
    justifyContent: "center",
  },
});
